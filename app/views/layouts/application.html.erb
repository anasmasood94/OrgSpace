<!DOCTYPE html>
<html>
  <head>
    <title>OrgSpace</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-white">
      <div class="container">
        <%= link_to "OrgSpace", root_path, class: "navbar-brand" %>

        <div class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <span class="navbar-text me-3">
              Welcome, <%= current_user.email %>
            </span>
            <%= link_to "Organization", organizations_path, class: "nav-link" %>
            <% if admin_organizations_for(current_user).count == 1 %>
              <%= link_to "Analytics", organization_analytics_path(admin_organizations_for(current_user).first), class: "nav-link" %>
            <% else %>
              <%= link_to "Analytics", analytics_path, class: "nav-link" %>
            <% end %>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <% if notice %>
          <div class="alert alert-success"><%= notice %></div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
